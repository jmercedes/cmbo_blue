<%= render '/refinery/content_page' %>

<!-- Nivo Slider -->
<div class="theme-default" id="slider">
    <% @page.images.each do |img| %>
        <%= image_tag img.url %>
    <% end %>
</div>

<div class="row">

<div class="span3">
<%= raw @page.content_for(:box_one) %>
</div>

<div class="span3">
<%= raw @page.content_for(:box_two) %>
</div>

<div class="span3">
<%= raw @page.content_for(:box_three) %>
</div>

</div>




<%= raw @page.content_for(:box_four) %>