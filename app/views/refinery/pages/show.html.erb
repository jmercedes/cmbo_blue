
<div class="row">
<div class="span3">

<ul>
  <% @page.children.each do |child| %>
      <li><%= link_to child.title, child.url %></li>
  <% end %>
</ul>



<%= raw @page.content_for(:side_body) %></div>
<div class="span8 dotted">

<h1><%= @page.title %></h1>
<%= raw @page.content_for(:body) %>

</div>



</div> 