<div class="span3 aside_column">
	
<nav id="recent_news_items">
  <h2>Noticias Recientes<%#= t('.recent_posts') %></h2>

    <% @items.each do |item| %>
      <p><strong><%= link_to item.title, refinery.news_item_path(item) %></strong></p>        
        <p><%= truncate  item.body, length: 150,
                       omission: "...", preserve_html_tags: true %>
                       
        </p>               
      <hr>

    <% end %>
</nav>
</div>